Language: Spanish (es)

Last maintainer: Alberto Serrano (alb.serrano@gmail.com)

Last update: 2015-10-11

1) Plone translation dictionary
Feel free to modify the list for your needs. Here it is:

     acquire        = adquirir / heredar (context dependant)
     action         = acción
     add            = agregar
     add-on         = complemento
     Add-on Products = Productos Adicionales
     advanced       = avanzado
     align center   = centrar
     align full     = justificar
     align left/right = alinear a la izquierda/derecha
     anchor         = hipervínculo
     area           = área
     auth/authentication = autenticación
     behavior       = comportamiento
     below          = siguiente / a continuación
     boilerplate    = modelo
     box            = casilla / cuadro (context dependant)
     brain          = autor
     Browse         = Navegar
     built-in       = integrado
     bundle         = paquete
     byline         = pie de autor
     cache          = caché
     caption (n)    = leyenda
     caption (v)    = añadir leyenda
     charset/character set = juego de caracteres
     check          = marcar / comprobar
     check in       = guardar
     check out      = retirar
     checkbox       = casilla de verificación
     click          = hacer clic
     collection     = colección
     comment        = comentario
     company        = empresa
     configuration, preference = configuración, preferencias
     content        = contenido
     contributor    = colaborador
     cookies        = cookies
     core Plone types = tipos básicos de Plone
     creator        = creador
     criteria       = criterios
     criterion      = criterio
     crop           = recortar
     deadline       = fecha límite
     default        = por defecto
     defaults       = configuración por defecto
     delete         = eliminar
     detail         = detalle
     disable        = deshabilitar
     discussion     = discusión
     draft          = borrador
     drawing        = dibujo
     effect         = tomar efecto (take effect)
     e-mail (also mail) = correo
     enable         = habilitar
     encoding       = codificación
     encrypt        = cifrar
     entry          = entrada
     external       = externo
     favorites      = favoritos
     feed           = fuente / fuente RSS
     feedback       = opinión
     field          = campo
     fieldset       = grupo de campos
     file           = archivo
     firewall       = cortafuegos
     folder         = carpeta
     folderish      = tipo carpeta
     front page     = página de portada
     fullscreen     = pantalla completa
     full-text search = búsqueda de texto completo
     guidelines     = guías
     head over      = ir / dirigirse
     hide           = ocultar
     home           = inicio
     host           = equipo
     hover          = situar el cursor sobre
     id             = id
     indent         = aumentar sangría
     inline edition = edición rápida
     input          = entrada
     Insufficient privileges = permisos insuficientes
     internal       = interno
     invalid        = no válido
     join form      = formulario de registro
     language       = idioma
     language neutral = neutral respecto al idioma
     launch         = lanzar
     lead image     = imagen principal
     line           = línea
     link           = enlace
     location       = ubicación
     log            = registro
     login (n)      = inicio de sesión
     login (v)      = iniciar sesión
     mail           = correo
     Mail Host      = servidor de correo
     mapping        = asociación
     media          = contenido multimedia
     membership     = pertenencia
     member         = miembro / usuario
     metadata       = metadatos
     missing        = falta
     mockup         = maqueta
     modify         = modificar
     move down      = bajar
     move up        = subir
     name           = nombre
     non-breaking space = espacio duro
     none           = ninguno
     old style      = viejo estilo
     on-the-fly     = al vuelo
     optional       = opcional
     organize       = organizar
     outdent        = disminuir sangría
     overlay        = capa
     overview       = visión general
     pack           = compactar
     path           = ruta
     pending        = pendiente
     pixel          = puntos
     plain text     = texto plano
     Please         = Por favor
     Plone          = Plone
     plugin         = complemento
     pointer        = cursor
     policy         = política
     popup          = emergente
     portlet        = portlet
     portrait       = foto
     prepend        = añadir al principio
     preview        = vista preliminar
     private        = privado
     privileges     = privilegios
     Properties Panel = Panel de Propiedades
     properties     = propiedades
     proxy          = proxy
     publishing history = histórico de publicación
     publishing     = publicación
     raw            = en crudo
     rearrange      = reorganizar
     redo           = rehacer
     refresh        = actualizar
     region         = área
     reject         = rechazar
     remove         = quitar
     render         = mostrar
     reply          = responder
     required       = requerido
     retract        = retirar
     reversed       = inverso
     reviewer       = revisor
     review         = revisar
     revision       = versión
     rich text      = texto enriquecido
     rights         = permisos
     role mapping   = asociación de roles
     ruler          = regla
     save           = guardar
     scale          = escalar	
     search         = buscar
     section        = sección
     sharing permissions = permisos de compartición
     sheet          = hoja
     shortcut       = atajo
     show           = mostrar
     site           = sitio
     Site 'From'    = remitente del sitio
     skin           = tema
     social media   = redes sociales
     spreadsheet    = libro / hoja de cálculo
     string         = cadena
     sub...         = sub...
     subfolder      = subcarpeta
     subject        = asunto
     submit         = enviar
     subtopic       = subtema
     successfully   = satisfactoriamente
     syndication    = sindicación
     tag            = etiqueta
     time and date  = fecha y hora
     tip            = consejo
     title          = título
     toggle         = cambiar a
     tool           = herramienta
     transaction    = transacción
     transcript     = transcripción
     TTW (through the web) = a través de la web
     undo           = deshacer
     uncheck        = desmarcar
     unique         = único
     update         = actualizar
     upload         = subir / subida
     URL            = URL
     valid          = válido
     validate       = validar
     visible        = visible
     web site       = sitio web
     web            = web
     wizard         = asistente
     workflow       = flujo de trabajo

2) Unwelcome terms

The following is the list of words in the target language that should not appear
in the translation. May be used as a notepad while translating, storing the
stubs of words that should be searched and replaced at the end of work:

     añadir        -> agregar
     borrar        -> eliminar
     compañía      -> empresa
     directorio    -> carpeta
     fichero       -> archivo
     lenguaje      -> idioma


3) Misc. language specific rules

Any translation notes you want to leave for future translators.

Spreadsheet should be translated as "Libro" when making reference to
Microsoft Excel documents, for respecting the well known and official existing
translation. However, when making reference to OpenOffice documents or any other
spreadsheet documents, the more generic term "Hoja de cálculo" should be used
instead.

