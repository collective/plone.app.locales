Ce document donne une politique de traduction pour Plone et les produits.

Language data:
Language: French

Last maintainer: Sebastien Douche (sdouche at gmail.com)

Last update: 14.07.2005

1) Plone translation dictionary:

     acquire = hériter / héritage
     action = action
     add-on products = Modules d'extension / Modules
     administrator = administrateur
     advanced = avancé(e)
     area  = zone
     authentication = authentificaton
     below = ci-dessous
     browse  = parcourir
     browser = navigateur
     change  = modifier
     channel  = canal
     check  = vérifier
     click = cliquer
     collection = collection / liste (selon le contexte)
     comment = commenter / commentaire
     configuration, preference = configuration, préférence
     content  = contenu
     content type  = type de contenu
     control = contrôle
     cookies  = cookies
     copyright  = droit d'auteur
     create  = créer
     creator  = créateur
     criteria  = critères
     criterion  = critère
     custom view   = vue personnalisée
     default   = défaut
     delete   = effacer
     detail   = information
     disable   = désactiver
     display  = afficher
     discussion  = discussion
     effective date = date d'activation
     e-mail /mail  = courriel
     email address = adresse courriel
     enable  = activer
     entry  = entrée
     error  = erreur
     event   = événement
     exception   = exception
     external  = externe
     favorites   = favoris
     feed  = flux
     feedback = retour
     flip  = flip
     firewall  = firewall
     folder = dossier, repertoire
     form  = formulaire
     front page = page d'accueil
     head over = vous pouvez aller
     home  = racine / accueil
     include  = inclure
     indice  = index
     integer = nombre entier
     input  = entrée / saisie
     insufficient privileges = Privilèges insuffisants
     item   = élément
     keywords = mots-clés
     line  = ligne
     link  = lien
     listing  = liste
     live search  = recherche dynamique
     log  = journal / rapport
     log in  = entrer / se signer
     log out  = quitter
     mail  = courriel
     mail host = serveur de courriel
     membership = gestion des membres
     member = membre
     metadata  = métadonnées
     modify = modifier
     name  = nom
     owner = propriétaire
     ownership = propriété (droits de propriétaire)
     optional  = optionnel
     organize = organiser
     overview  = aperçu
     page = document, page (selon contexte)
     parent  = parent
     pending  = en attente
     pixel = pixel
     placeholder = emplacement
     please  = veuillez ...
     Plone  = Plone
     policy = règle
     portal  = portail
     portrait = portrait
     private = privé
     privileges = privilèges
     panel = panneau
     properties panel = Panneau de propriétés
     properties = propriétés
     proxy  = passerelle
     publishing history = historique de publication
     publishing = publication
     published = publié
     refresh = actualiser
     relevance  = pertinence
     reject = rejeter
     remove = enlever / supprimer
     reply = répondre / réponse
     required = requis / obligatoire
     retract = retirer la soumission
     reviewer = modérateur
     review = modérer / valider
     rights  = droits
     save  = sauvegarder
     search  = rechercher
     searchable text = recherche par texte ?
     search term = terme de recherche
     section = section
     send mail  = envoyer un message
     settings = paramêtres, options
     setup = configurer, paramétrer
     sharing permissions = permissions de partage
     show = rendre visible
     smart folder  = dossier automatique
     site  = site
     site map  = plan du site
     skin   = thème /  habillage
     sub...   = sous...
     subfolder = sous-dossier, sous-répertoire
     subject = sujet
     submit = soumettre
     syndication = syndication
     template = gabarit
     title  = titre
     tool = outil
     traceback = traces
     transaction = transaction
     transcript = rapport d'installation
     type = type
     undo = annuler
     unique  = unique
     update = mise a jour
     upload  = transférer sur le serveur
     up to = revenir à / retour niveau supérieur ?
     URL  = URL
     user = utilisateur
     validator  = validateur
     view  = vue / voir
     visible  = visible
     web site = site web
     web = Web
     workflow  = processus documentaire
2) Unwelcome terms
None

3) Misc. language specific rules
This is work in progress...
